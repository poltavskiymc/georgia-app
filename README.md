# Грузия — трэвел-компаньон

Оффлайн-PWA для поездки по Грузии на машине: ИИ-помощник, переводчик, разговорник,
гид по местам и еде с навигатором, план поездки с чек-листами, конвертер валют.
Без сборщика — чистый HTML/CSS/JS, раздаётся любым статическим сервером.

## Вкладки

Пять вкладок внизу; настройки — ⚙️ в шапке (не вкладка).

| Вкладка | Что внутри |
|---|---|
| 💬 **Чат** | ИИ-помощник «Гоги» (DeepSeek), несколько чатов |
| 🗣️ **Язык** | под-вкладки: 🌐 Перевод и 🗣️ Фразы |
| 🧭 **Гид** | под-вкладки: 🗺️ Места (регионы, точки, навигатор, энциклопедия) и 🍽️ Еда (блюда) |
| ✅ **План** | 🧳 Сборы и граница · 📍 Хочу посмотреть · 🍽️ Хочу попробовать (+ оценка в хинкалях) |
| 💸 **Деньги** | конвертер ₾ ⇄ ₽ ⇄ $ ⇄ € (пересчёт во все стороны), курсы, практические советы |

«Гид» — источник, «План» — приёмник: у каждой точки и блюда есть кнопка ＋, которая
кладёт их в соответствующий список «Плана».

## Структура

```
georgia-app/
├── index.html            # разметка всех вкладок + подключение css/js по порядку
├── manifest.webmanifest  # PWA-манифест (иконки, имя, цвета)
├── sw.js                 # service worker: stale-while-revalidate + авто-обновление
├── README.md             # этот файл
├── BACKLOG.md            # что дальше
├── css/
│   └── styles.css        # все стили (переменные темы, светлая/тёмная)
├── js/                   # скрипты грузятся в этом порядке (глобальные функции, без модулей)
│   ├── data.js           # КОНТЕНТ: PHRASES, ROUTE, TIPS, FOOD, PACK, SYSTEM. Только данные.
│   ├── util.js           # ХЕЛПЕРЫ: esc, byteLen/fmtBytes, fmtTs, haversine/fmtDist, speak
│   ├── traffic.js        # учёт трафика: traffic, addTraffic(), renderTraffic()
│   ├── nav.js            # вкладки show() + под-вкладки (.tabs/.subview)
│   ├── phrases.js        # «Язык» → Фразы + избранное: renderPhrases(), favs
│   ├── plan.js           # вкладка «План»: planHas/planToggle/planRate, renderPlan(). ДО route/food.
│   ├── trip.js           # совместная поездка: синк «Плана» между телефонами по коду. ПОСЛЕ plan.js.
│   ├── route.js          # «Гид» → Места: navLink(), applyDist()/OSRM, геолокация, кнопка ＋
│   ├── wiki.js           # энциклопедия региона (Википедия, кэш в localStorage)
│   ├── food.js           # «Гид» → Еда: блюда, фото из вики, кнопка ＋ и оценка 🥟
│   ├── money.js          # «Деньги»: конвертер 4 валют + fetchRates() (ручной курс > сетевого)
│   ├── chat.js           # вкладка «Чат»: send(), renderChat(), refreshKeyHint(), history
│   ├── translate.js      # «Язык» → Перевод: translate() (DeepSeek → en/ka + озвучка)
│   └── settings.js       # ⚙️: ключ DeepSeek, сброс трафика, версия. Грузится ПОСЛЕДНИМ.
├── icons/                # иконки приложения
└── server/               # сервер синхронизации плана (Deno Deploy + Deno KV), деплоится отдельно
    ├── main.ts           # PUT /t/:id · GET /t/:id/rev · POST /t/:id (мерджит и отдаёт общий план)
    └── deno.json         # задачи dev/deploy
```

## Совместная поездка (синк «Плана» между телефонами)

Один жмёт в ⚙️ «Создать поездку» и получает код вида `K7M4-QP2X`, кнопка «Поделиться» кидает
второму ссылку `…/#trip=K7M4-QP2X` (открыл — приложение само предложит присоединиться).
Синхронизируются только три списка «Плана»: сборы, места, блюда с оценками. Чат, ключ
DeepSeek и счётчик трафика — личные, на сервер не уходят. Код — это и весь пароль.

**Как устроено.** У каждого пункта есть `id` (координаты точки / имя блюда — стабильные) и `ts`
последней правки. Синк — один POST: шлём воркеру всё своё состояние, он объединяет его с тем,
что лежит в KV (union по `id`, при конфликте побеждает больший `ts`), и возвращает результат,
которым мы и заменяем локальный. Поэтому оба человека могут добавлять и отмечать одновременно,
и никто никого не затирает.

⚠️ **Ответ сервера нельзя применять «в лоб».** Пока запрос летит (сотни миллисекунд на реальной
сети), человек успевает нажать галочку — сервер про неё ещё не знает, и его ответ бы её затёр:
тап пропадал молча. Поэтому `planApplyRemote()` **мержит** ответ с текущим локальным состоянием
тем же правилом (`mergeById`, побеждает больший `ts`), а не присваивает его. Правка, сделанная
во время запроса, свежее — она выживает, а `trip.js` её потом допошлёт (`tripChanged > started`).
Локально этот баг не воспроизводится: там ответ приходит за миллисекунду. Ловится только тестом
против настоящего сервера.

⚠️ **Удаление — это `{del:true}`, а не пропажа строки.** Если просто выкинуть пункт из массива,
второй телефон при следующем синке пришлёт его обратно — для него это «новый пункт, которого
у тебя нет». Поэтому убранные пункты остаются в списке «надгробиями» и фильтруются при отрисовке
(`planLive()`). По той же причине снятая галочка в сборах хранится как `{done:false, ts}`, а не
удаляется — иначе она вернулась бы отмеченной.

**Оффлайн** правки просто копятся в localStorage и уезжают при появлении сети (`online`,
`visibilitychange`). Раз в 45 с приложение спрашивает у сервера `GET /t/:id/rev` — это пара сотен
байт; полный синк дёргается, только если версия на сервере изменилась.

⚠️ **Почему Deno Deploy, а не Cloudflare Workers** (изначально хотели их): `*.workers.dev`
не открывается из России — DNS резолвится, а TCP-соединение не устанавливается. Сервер на нём
был бы недоступен обоим телефонам до самой границы. `*.deno.dev` открывается. Прежде чем менять
площадку, проверь доступность так:

```bash
curl -sS -o /dev/null -w '%{http_code}\n' --max-time 8 https://<домен>/
```

**Локальный запуск и деплой** (нужен бесплатный аккаунт Deno Deploy, вход через GitHub):

```bash
cd server
deno task dev                              # поднимет на localhost:8000 с локальным KV
deployctl login                            # один раз, откроет браузер
deployctl deploy --project=georgia-trip --prod main.ts
```

Адрес из последней строки (`https://georgia-trip.deno.dev`) → в `TRIP_API` в начале `js/trip.js`.
Пока `TRIP_API` пуст, синк просто выключен: карточка в ⚙️ честно пишет, что сервер не настроен,
всё остальное работает как раньше.

## Как это связано (важно при правках)

- **Порядок скриптов в `index.html` важен.** Модули общаются через глобальные функции,
  а не через import. Правило: файл можно двигать, пока функции, которые он *вызывает
  на загрузке*, определены в файлах выше. Цепочка зависимостей:
  `data → util → traffic → nav → phrases → plan → route → wiki → food → money → chat → translate → settings`.
- **`plan.js` грузится ДО `route.js` и `food.js`:** те при отрисовке спрашивают у него
  `planItem()`/`planRating()` и вешают `planToggle()`/`planRate()`. Обратная связь (пункт убрали
  или оценили прямо в «Плане») идёт событием `planchange` на `document` — гид перерисовывает
  свои кнопки. Состояние плана: `plan_see`, `plan_eat`, `pack_done` в localStorage;
  id пункта стабильный (координаты точки / название блюда), чтобы позже можно было
  мерджить план между двумя телефонами.
- Общие функции живут в **util.js** (`esc`, `speak`, `haversine`, `fmtTs`, `byteLen`…).
  Новую утилиту, нужную двум+ вкладкам, добавляй туда.
- Любой сетевой запрос оборачивай `addTraffic(byteLen(body), byteLen(raw))` — иначе
  вкладка «Ключ» покажет неполный расход.
- **Контент правится в `data.js`** (новые фразы, точки маршрута, советы) — трогать
  логику при этом не нужно.

## Кэш и версии

При изменении **любого** файла в `css/` или `js/` подними номер версии в **трёх** местах,
они должны совпадать:

1. `const VER = 'N'` в `sw.js` (из него собирается имя кеша `georgia-vN`);
2. `?v=N` у всех `<link>`/`<script>` в `index.html`;
3. `APP_VERSION` в `js/settings.js` (видно внизу ⚙️ — по этой строке проверяют, что телефон обновился).

Почему так (сборка 21 залипла у пользователя именно на этом):

- **HTML — network-first.** Есть сеть — всегда свежая разметка; нет сети — из кеша. Пока
  `index.html` отдавался из кеша, телефон мог неделю жить на старой версии и не узнать об обновлении.
- **css/js — stale-while-revalidate** (мгновенно из кеша, обновление в фоне), но подключаются
  с `?v=N`. Свежий HTML просит `js/plan.js?v=23` — такого URL в кеше нет, значит файл гарантированно
  приедет из сети. Залипнуть на старом js невозможно.
- Ручная кнопка «Обновить приложение» (⚙️) сносит все кеши и перезагружает — аварийный выход,
  если что-то всё же пошло не так.
- Офлайн полностью рабочий: без сети всё отдаётся из кеша, сетевые запросы (ИИ/курс/OSRM)
  просто не выполнятся до появления связи.

## Проверка правок без браузера

Headless Chrome в этом окружении зависает — не использовать. Вместо него: `node --check js/*.js`
плюс прогон настоящего `index.html` в **jsdom** (скрипты подключать как реальные `<script>`,
а не через `eval` — иначе top-level `const` из `data.js` не попадёт в общую область видимости).
Так проверяются клики, localStorage и синхронизация «Гид» ⇄ «План».

## Ограничения окружения

- **Геолокация** (расстояния в «Гид → Места») и **service worker** (офлайн)
  в браузере работают только на **https или localhost**. По `http://<LAN-IP>` они
  блокируются. Для полноценной работы на телефоне — раздавать по https (напр. хостинг
  вроде Netlify/Cloudflare Pages) или через localhost.
- **Расстояния по дорогам** берутся с публичного демо-сервера OSRM
  (`router.project-osrm.org`) без ключа — годится для пары пользователей; при недоступности
  показывается расстояние по прямой (с «~»).
- **Распознавание по фото** пока не сделано: у DeepSeek API нет vision, нужна отдельная
  vision-модель (Gemini/OpenAI/Claude).

## Внешние сервисы

- **DeepSeek** (`api.deepseek.com`) — чат и переводчик. Ключ хранится в localStorage телефона.
- **open.er-api.com** — курсы: один запрос с базой GEL отдаёт RUB, USD и EUR разом.
- **router.project-osrm.org** — дорожные расстояния.
- Навигаторы: Google Maps, Яндекс.Карты, Maps.me (ссылки-маршруты, открываются во внешнем приложении).
